<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>@ViewBag.Title Home | E-Shopper</title>
    @*<title>@ViewData["Title"] Home | E-Shopper</title>*@

    @*@await Html.PartialAsync("Partial/_Styles")*@
    <partial name="Partial/_Styles" />
    @await RenderSectionAsync("Styles", false)
    <link rel="apple-touch-icon-precomposed" href="images/ico/apple-touch-icon-57-precomposed.png">
</head><!--/head-->

<body>
    <partial name="Partial/_Header" />

    @RenderBody()

    <partial name="Partial/_Footer" />

    <partial name="Partial/_Scripts" />

    @await RenderSectionAsync("Scripts",false)
</body>
</html>